{endpoint}firstmodule:helloEP{/endpoint}
==Detail==
This hello endpoint simply formats the value argument it receieves and returns it as a string response

==Resource Identifier Syntax==
The endpoint accepts requests which match the identifier syntax of ```res:/firstmodule/helloservice/ANY_OTHER_TEXT```

The ```ANY_OTHER_TEXT``` is captured as the value argument passed into the endpoint.